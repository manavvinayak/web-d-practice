const weather = prompt("What's the weather like? ")

switch(weather) {
    case "sunny":
        alert("Cover up! ")
         
        case "rainy":
            alert("Umbrella time! ")

            case "dusty":
                alert("Stay inside at home ")

                default:
                    alert("Not supported type! ")
}